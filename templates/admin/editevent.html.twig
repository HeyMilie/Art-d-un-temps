{% extends 'bo_base.html.twig' %}

{% block title %}Art d'un temps - Evénement {{ event.titre }}{% endblock %}

{% block content %}
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb bg-dark">
            <li class="breadcrumb-item"><a href="{{ path("accueil")}}">Accueil</a></li>
            <li class="breadcrumb-item"><a href="{{ path("profil_admin")}}">Profil de {{app.user.username}}</a></li>
            <li class="breadcrumb-item"><a href="{{ path("admin_evenements")}}">Liste des événements</a></li>
            <li class="breadcrumb-item active" aria-current="page">Modifier l'événement {{ event.titre }}</li>
        </ol>
    </nav>
    <h1>modifier l'événement</h1>
    <h2 class="text-center center h2-profil">{{ event.titre }}</h2>
        <div class="col-lg-6 mx-auto pt-3 border rounded bg-white">
            {{ form(eventForm)}}
            </div>
{% endblock %}
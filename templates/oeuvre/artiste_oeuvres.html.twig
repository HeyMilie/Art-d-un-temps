{% extends 'bo_base.html.twig' %}

{% block title %}Art d'un temps - Mes oeuvres {% endblock %}

{% block content %}
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb bg-dark">
            <li class="breadcrumb-item"><a href="{{ path("accueil")}}">Accueil</a></li>
            <li class="breadcrumb-item"><a href="{{ path("profil_artiste")}}">Profil de {{app.user.username}}</a></li> 
            <li class="breadcrumb-item active" aria-current="page">Mes oeuvres</li>
        </ol>
    </nav>

    <h1>Mes oeuvres</h1>

    <table class="table mt-3">
        <thead class="bg-light">
                <th>Reference</th>
                <th>Categorie</th>
                <th>Nom_oeuvre</th>
                <th>Année</th>
                <th>Prix</th>
                <th>Photo</th>
                <th>Stock</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for oeuvre in oeuvres %}
            <tr class="text-table">
                <td>{{ oeuvre.reference }}</td>
                <td>{{ oeuvre.categorie }}</td>
                <td>{{ oeuvre.nomOeuvre }}</td>
                <td>{{ oeuvre.annee ? oeuvre.annee|date('Y-m-d') : '' }}</td>
                <td>{{ oeuvre.prix }}</td>
                <td>{{ oeuvre.photo }}</td>
                <td>{{ oeuvre.stock }}</td>
                <td>
                    <a href="{{ path('artiste_oeuvre_show', {'id': oeuvre.id}) }}" class="link-action"><i class="fas fa-eye"></i></a>
                    <a href="{{ path('artiste_oeuvre_edit', {'id': oeuvre.id}) }}" class="link-action"><i class="fas fa-edit"></i></a>
                </td>
            </tr>
        {% else %}
            <tr class="text-table">
                <td colspan="10" class="text-table">Aucune oeuvre trouvée</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_oeuvre_new') }}" class="btn">Ajouter une oeuvre</a>
{% endblock %}